# Handoffs Log

Append-only. One line per event. Format:
```
[<ISO-8601 UTC>] FROM→TO: <task-id> <message>
```

---

[2026-05-23T18:45:00Z] A→B: backlog seeded. CM-01, CM-02 are ready to start in parallel (no inter-dep). Schemas live in `lib/types.ts`. See `coordination/AGENT_B_PROMPT.md` for your loop.
[2026-05-23T18:45:00Z] A→C: T-00 ready to start now. T-01 through T-05 unblock as B finishes each CM. See `coordination/AGENT_C_PROMPT.md` for your loop.

[2026-05-24T01:48:32Z] B→A: started CM-01
[2026-05-24T01:49:10Z] C→A: started T-00 (Vitest + Playwright + CI)
[2026-05-24T01:52:37Z] B→C: CM-01 ready — files: components/classmap/ClassMapForm.tsx, components/ui/{input,select,checkbox,textarea,label}.tsx — exercise: import { ClassMapForm } from "@/components/classmap/ClassMapForm"; render with onSubmit prop. Validation cases for T-01: childAge<3 or >18, hoursPerWeek<2 or >40, subjects empty, childName >40 chars, notes >500 chars, valid submit yields parsed LessonPlanInput. Optional string fields (childName/state/notes) coerce empty → undefined before Zod runs.
[2026-05-24T01:52:54Z] C→A: T-00 verified — added vitest.config.mts, playwright.config.ts, tests/setup.ts, tests/unit/{sanity,types}.test.ts (27 passing, real Zod parse of lib/types.ts), tests/e2e/smoke.spec.ts (1 passing against next dev on :3100), .github/workflows/test.yml (lint+vitest job and playwright job). Coverage: zero production code touched; types regression backfilled against A-02's schemas.
[2026-05-24T01:52:54Z] C→B: picking up CM-01 next
[2026-05-24T01:54:30Z] C→A: FYI process note — T-00 staged files were absorbed into B's commit f20588a (CM-01) due to parallel work in same worktree. Code is intact, but git history attributes T-00 to CM-01. Recommend worktrees per agent (per README) or a serialize-commits convention if this matters.
[2026-05-24T01:56:40Z] B→A: started CM-02
[2026-05-24T01:57:03Z] C→A: CM-01 verified — tests/unit/ClassMapForm.test.tsx (14/14 passing). Covers render, happy path with defaults, optional-string coercion to undefined, trimmed pass-through, childAge bounds (incl. non-integer), hoursPerWeek bounds, childName length, notes length (paste), empty subjects, defaultValues override. T-01 also DONE_PENDING_A. Filed ISS-01 (P2 a11y nit, non-blocking).
[2026-05-24T01:57:03Z] C→B: CM-01 PASS. ISS-01 (P2) optional polish — drop redundant `role="group" aria-label="Subjects"` on inner div, fieldset+legend already covers it.
[2026-05-24T01:57:44Z] B→C: ISS-01 fix ready for re-test — removed inner role="group" aria-label="Subjects" on components/classmap/ClassMapForm.tsx; fieldset/legend remains as the sole accessible group.
[2026-05-24T01:59:29Z] C→B: ISS-01 verified and CLOSED — tightened assertion to `toHaveLength(1)`, 41/41 passing.
[2026-05-24T01:59:29Z] C→A: filed ISS-02 (P2) — tsconfig edit at 01:57:29Z added `tests/**`, `vitest.config.mts`, `playwright.config.ts` to `exclude`. Broke `@/*` alias resolution in test files (vite-tsconfig-paths reads tsconfig include rules). Worked around in vitest.config.mts by hard-coding alias. Side cost: `npm run lint` no longer typechecks test files. Recommend reverting the exclude or adding a tsconfig.test.json.
[2026-05-24T02:00:30Z] A→C: signed off T-00 and T-01 → DONE. Signed off CM-01 → DONE. ISS-02 acknowledged, my fault, claimed as A-08; will land tsconfig.test.json so you can remove the hard-coded alias in vitest.config.mts.
[2026-05-24T02:00:30Z] A→B: nice work on CM-01 + ISS-01. CM-02 unblocked and is yours. When CM-02 is NEEDS_TEST, CM-03 unblocks (depends on CM-02, A-02 done, A-03 done). For CM-03, the API route is at app/classmap/api/generate/route.ts (POST { input } → returns LessonPlan). In demo mode, call `getDemoPlan(input)` from `@/lib/demo-data` instead — gate on `isDemoMode` from `@/lib/env`.
[2026-05-24T02:00:30Z] A→ALL: Repo is live at https://github.com/markjeromecruz/classmap. Pages enabled (build_type=workflow). Demo URL will be https://markjeromecruz.github.io/classmap/ once the rerun of "Deploy demo to GitHub Pages" succeeds (the first run failed because Pages wasn't enabled yet).
[2026-05-24T02:00:30Z] A→ALL: PROCESS UPDATE — to avoid the `git add -A` collision pattern, agents should stage by explicit path (`git add <file1> <file2>`) and never `git add .` or `git add -A` unless the working tree was clean at the start of the loop. This protects in-flight work from other agents. Will update AGENT_*_PROMPT.md to enforce.
[2026-05-24T02:02:15Z] B→C: CM-02 ready — files: components/classmap/{PlanCard.tsx,PlanBoard.tsx,subject-meta.ts}, components/ui/badge.tsx — exercise: render `<PlanBoard plan={fixture} />` with a 5-day LessonPlan. Verify: (1) all 5 day columns render in DAYS order (Mon..Fri), (2) sessions count badge per day, (3) each PlanCard shows subject Badge + minutes + title + description + materials chips, (4) subject accent color via border-l-4 + colored badge (data-subject="<subject>"), (5) plan.summary renders above grid if present. Selectors: data-slot="plan-board", data-slot="plan-day" data-day="Mon", data-slot="plan-card" data-subject="math".
[2026-05-24T02:03:02Z] C→A: idle backfill — tests/unit/demo-data.test.ts (11 tests, 52/52 suite). Pins all 3 DEMO_PLANS to lessonPlanSchema, locks Mon..Fri day order, session bounds, and getDemoPlan age-band dispatch (≤8 EARLY, 9–12 UPPER, ≥13 TEEN). Committed as 0a832ee.
[2026-05-24T02:03:02Z] C→A: started verifying CM-02
[2026-05-24T02:05:30Z] A→C: A-08 landed — `tsconfig.test.json` extends root and re-adds full include + `@/*` paths so vite-tsconfig-paths can resolve aliases across the whole tree (not just tests/). Your defensive `resolve.alias` in vitest.config.mts can stay as belt-and-suspenders or be removed; both work. 52/52 local.
[2026-05-24T02:05:30Z] A→C: fixed CI playwright build — `test.yml` had `NEXT_PUBLIC_DEMO_MODE: "true"` on the build step, which clashed with `dynamic = "force-dynamic"` in the api route under `output: "export"`. Dropped DEMO_MODE from the build step (kept it on the test:e2e step where the dev server actually runs). Build is a smoke check; the dev server doesn't use the build output.
[2026-05-24T02:05:30Z] A→ALL: Pages site is LIVE — https://markjeromecruz.github.io/classmap/ returns 200. Landing page (portfolio) deployed via deploy-pages workflow. The ClassMap form route will work once B finishes CM-03 (form wiring) and the next deploy runs.
[2026-05-24T02:05:57Z] C→A: CM-02 verified — tests/unit/PlanBoard.test.tsx (17/17 passing; suite 69/69). PlanBoard: 5 Mon..Fri columns, summary toggle, session count + pluralization, per-day card counts and subject ordering, missing-day fallback (renders empty column), aria-label region, data-plan-id. PlanCard: subject badge + title + description + minutes, formatMinutes (45 min / 1h / 1h 30m), materials chips in labelled list (omitted when empty), accent class, PE/Language short labels. CM-02 + T-02 → DONE_PENDING_A.
[2026-05-24T02:05:57Z] C→A: ISS-02 verified CLOSED with A-08 — also kept my `resolve.alias` belt-and-suspenders in vitest.config.mts (documented as hedge against tsconfig drift). No new issues filed.
[2026-05-24T02:08:21Z] C→A: idle backfill — tests/unit/AppCard.test.tsx (8 tests, suite 77/77). Covers live+href (link wrapper + Open demo affordance), live without href (non-interactive fallback), in-progress and coming-soon variants (no link, no Open demo, correct status label), title/tagline/description/highlights rendering, zero-highlights edge case. Filed ISS-03 (P2) — shadcn `CardTitle` is a `<div>`, not a heading, so AppCard + PlanCard titles aren't reachable by screen-reader heading nav.
[2026-05-24T02:08:55Z] B→A: started CM-03
[2026-05-24T02:10:29Z] B→C: CM-03 ready — files: app/classmap/page.tsx, app/classmap/result/page.tsx, components/classmap/ClassMapFlow.tsx — exercise: navigate to /classmap (portfolio link) or /classmap/result. Fill form, click Generate. Demo mode (NEXT_PUBLIC_DEMO_MODE=true) returns a canned plan via getDemoPlan and renders PlanBoard inline below form; submit label becomes "Regenerate plan". Live mode POSTs JSON body to /classmap/api/generate. Error path renders a destructive alert at data-slot="classmap-flow-error". Reset via "Clear" button (data-status flips back to idle). Selectors: data-slot="classmap-flow"[data-status], "classmap-flow-form"[aria-busy], "classmap-flow-result", "classmap-flow-error". For T-03 mock /classmap/api/generate with a LessonPlan fixture; or set forceDemo prop / DEMO env to skip the network call. Built clean — both pages prerender static, api route stays dynamic.
[2026-05-24T02:11:14Z] B→A: started CM-04
[2026-05-24T02:14:54Z] B→C: CM-04 ready — files: lib/storage.ts, components/classmap/{ClassMapFlow.tsx,SavedPlansList.tsx}, app/classmap/{page.tsx,saved/page.tsx} — exercise: (1) Generate a plan at /classmap, hit "Save plan" — button flips to "Saved" + disabled. (2) Navigate to /classmap/saved — plan renders via PlanBoard with header "<name>s week, age N" and saved time. (3) Click Delete — plan disappears from list. (4) Re-generate same id → "Save plan" appears again. Storage key: classmap:saved-plans:v1. Reading parses each entry through lessonPlanSchema — corrupt entries silently dropped. Empty state has data-slot="saved-plans-empty"; populated list has data-slot="saved-plans-list" data-count="N". Per-item: data-slot="saved-plan" data-plan-id; delete via [data-testid="saved-plan-delete"]. Save button: [data-testid="classmap-flow-save"]. For T-04, set window.localStorage in test setup, render <SavedPlansList /> directly, or drive the full flow via the saved page.
[2026-05-24T02:15:00Z] C→A: idle backfill — tests/unit/claude.test.ts (9 tests, suite 88/88). Mocks the spawn boundary only (justified at process boundary, via vi.hoisted to share the mock between named import and default export); the rest of generateLessonPlan runs for real. Pins: bare JSON parse, ```json fenced strip, bare ``` fenced strip, outer {…} fallback in prose, spawn argv contract (claude binary, -p, --append-system-prompt, --output-format text, prompt mentions subjects + total minutes target), non-JSON rejection, schema-invalid rejection, non-zero exit (surfaces stderr), spawn ENOENT (failed to spawn).
[2026-05-24T02:15:00Z] C→A: ISS-03 → OPEN (partial). A-09 swapped AppCard to a direct <h2> (good), so portfolio cards are reachable. PlanCard still uses shadcn CardTitle (still a <div>). Root cause in components/ui/card.tsx unchanged. AppCard test re-aligned to the redesign (note: A's parallel edit landed; my fuller version was rejected, A's 10-test version is on disk and green).
[2026-05-24T02:15:00Z] C→A: starting verification of CM-03 and CM-04 (both NEEDS_TEST).
[2026-05-24T02:16:56Z] B→C: ISS-03 fix ready for re-test — PlanCard renders session title as direct <h4>, matching A-09 pattern; CardTitle no longer imported. Sits under PlanBoard day <h3>. Suggested assertion: within a PlanCard, getByRole("heading", {level: 4, name: session.title}).
[2026-05-24T02:17:46Z] B→A: started CM-06
[2026-05-24T02:18:40Z] C→A: CM-03 verified — tests/unit/ClassMapFlow.test.tsx (10 tests). Initial render (data-status=idle), demo happy path with PlanBoard rendered, submit-label flip to "Regenerate plan", Clear reset, save persistence and "Saved" disabled state, save reset on regenerate (new id), live fetch happy/error/throw paths (vi.stubGlobal on fetch — justified at network boundary). CM-03 + T-03 → DONE_PENDING_A.
[2026-05-24T02:18:40Z] C→A: CM-04 verified — tests/unit/storage.test.ts (13 tests, real localStorage) + tests/unit/SavedPlansList.test.tsx (6 tests). Storage: empty state, savePlan ordering (newest first), dedupe-by-id (in-place replace + move to head), deletePlan, delete missing is no-op, documented key. Corruption: bad JSON / non-array / schema-invalid entries silently dropped. List: empty placeholder, populated with data-count + per-item data-plan-id, child-name vs fallback headers, delete updates DOM + storage, corruption-resilient render. CM-04 + T-04 → DONE_PENDING_A.
[2026-05-24T02:18:40Z] C→B: ISS-03 verified CLOSED — added regression assertion `getByRole("heading", {level: 4, name: session.title})` in tests/unit/PlanBoard.test.tsx; 116/116 passing.
[2026-05-24T02:19:54Z] B→C: CM-06 ready — file: app/globals.css (extended @media print block). To verify: open /classmap with NEXT_PUBLIC_DEMO_MODE=true, generate a plan, hit browser print preview. Expect: (1) form section hidden, (2) Save/Clear/Delete buttons hidden, (3) day columns stacked vertically (not 5-col grid), (4) cards have no background fill, badges are outlined not filled, (5) plan summary visible in black, (6) headings (h1-h4) avoid breaking right before page end. /classmap/saved should also print cleanly (Delete buttons hidden). Smoke-testable manually in Playwright with `page.emulateMedia({ media: "print" })` then screenshot.
[2026-05-24T02:20:37Z] B→A: started CM-05
[2026-05-24T02:23:11Z] B→C: CM-05 ready — files: components/classmap/{PlanBoardSkeleton.tsx,ClassMapFlow.tsx,SavedPlansList.tsx}, components/ui/{skeleton.tsx,alert.tsx} — exercise: (1) submit form on /classmap; while in-flight, ClassMapFlow renders <PlanBoardSkeleton/> (5 day columns of pulsing card placeholders, data-slot="plan-board-skeleton" aria-busy aria-live). Demo mode resolves synchronously so the skeleton is fleeting — test via the loading branch directly or by suspending getDemoPlan. (2) Force the live-fetch error path: error renders as shadcn Alert variant=destructive at data-slot="classmap-flow-error" (replaces the prior custom div, role="alert" preserved). (3) Navigate to /classmap/saved with localStorage seeded post-mount — pre-hydration shows three Skeleton bars at data-slot="saved-plans-loading" aria-busy aria-live. Build still 4 routes (/, /classmap, /classmap/result, /classmap/saved static; api dynamic).
[2026-05-24T02:24:18Z] C→A: CM-06 verified — tests/unit/print-stylesheet.test.ts (12 source-level tests on the @media print block in app/globals.css; pins hidden chrome selectors, display:block grid, page-break-inside avoid on day/card, outline badges, body B&W, h1..h4 page-break-after) + tests/e2e/print.spec.ts (1 chromium spec via page.emulateMedia({media:"print"}) asserts form + submit hidden). Day-grid stacking under live print emulation was attempted but submit didn't reliably trigger via Playwright (defaultValues from react-hook-form didn't reach uncontrolled DOM by click time) — covered at source level instead. Note documented in the e2e file for follow-up. CM-06 → DONE_PENDING_A.
[2026-05-24T02:24:18Z] C→A: picking up CM-05 next (also NEEDS_TEST).
[2026-05-24T02:26:57Z] C→A: CM-05 verified — tests/unit/PlanBoardSkeleton.test.tsx (6 tests) + 2 new tests in tests/unit/ClassMapFlow.test.tsx. Skeleton: documented data-slot="plan-board-skeleton" + aria-busy + aria-live=polite + aria-label, 5 day-skeleton cols with day labels, defaults to 3 placeholder cards per day (configurable), uses shadcn Skeleton primitive (animate-pulse). ClassMapFlow loading: deferred-fetch test asserts skeleton appears during in-flight, form aria-busy=true, skeleton removed on success. Error: now shadcn Alert (role="alert", data-slot overridden to "classmap-flow-error"), AlertTitle "Couldn't generate a plan." + AlertDescription with server message. SavedPlansList saved-plans-loading slot: present in source per B's contract; in-flight timing not deterministically testable under React 19 — accepted gap, low risk (regression would be a brief visual blink). CM-05 → DONE_PENDING_A.
